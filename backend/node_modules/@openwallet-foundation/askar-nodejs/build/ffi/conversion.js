"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.encryptedBufferStructToClass = exports.secretBufferToUint8Array = exports.byteBufferToUint8Array = exports.decodeHandleList = exports.uint8ArrayToByteBufferStruct = void 0;
const askar_shared_1 = require("@openwallet-foundation/askar-shared");
const koffi = __importStar(require("koffi"));
const primitives_1 = require("./primitives");
const uint8ArrayToByteBufferStruct = (buffer = new Uint8Array(0)) => {
    return { data: buffer, len: buffer.length };
};
exports.uint8ArrayToByteBufferStruct = uint8ArrayToByteBufferStruct;
const decodeHandleList = (handleList) => {
    // With koffi, pointer data needs to be decoded
    const { data, len } = handleList;
    // If data is a koffi external pointer, decode it as uint8 array
    const decoded = koffi.decode(data, primitives_1.FFI_USIZE, len);
    return decoded;
};
exports.decodeHandleList = decodeHandleList;
const byteBufferToUint8Array = (byteBuffer) => {
    // With koffi, pointer data needs to be decoded
    const { data, len } = byteBuffer;
    // Check if data is already a Uint8Array
    if (data instanceof Uint8Array) {
        return data.slice(0, len);
    }
    // If data is a koffi external pointer, decode it as uint8 array
    const decoded = koffi.decode(data, primitives_1.FFI_UINT8, len);
    return decoded;
};
exports.byteBufferToUint8Array = byteBufferToUint8Array;
exports.secretBufferToUint8Array = exports.byteBufferToUint8Array;
const encryptedBufferStructToClass = ({ secretBuffer, tagPos, noncePos }) => {
    const buffer = Uint8Array.from((0, exports.secretBufferToUint8Array)(secretBuffer));
    return new askar_shared_1.EncryptedBuffer({ tagPos, noncePos, buffer });
};
exports.encryptedBufferStructToClass = encryptedBufferStructToClass;
//# sourceMappingURL=conversion.js.map