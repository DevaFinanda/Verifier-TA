"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.NodeJSAskar = void 0;
const askar_shared_1 = require("@openwallet-foundation/askar-shared");
const koffi = __importStar(require("koffi"));
const ffi_1 = require("./ffi");
const library_1 = require("./library");
function handleNullableReturnPointer(returnValue) {
    const value = returnValue[0];
    if (value === null || value === undefined)
        return null;
    return value;
}
function handleReturnPointer(returnValue) {
    const value = Array.isArray(returnValue) ? returnValue[0] : returnValue;
    if (value === null || value === undefined) {
        throw askar_shared_1.AskarError.customError({ message: 'Unexpected null pointer' });
    }
    return value;
}
class NodeJSAskar {
    constructor() {
        this.callbackId = 1;
        this.promisify = async (method, callbackType) => {
            return new Promise((resolve, reject) => {
                let keepAlive;
                const scheduleKeepAlive = () => {
                    keepAlive = setImmediate(scheduleKeepAlive);
                };
                scheduleKeepAlive();
                const registeredCallback = koffi.register((_cbId, errorCode, result) => {
                    clearImmediate(keepAlive);
                    koffi.unregister(registeredCallback);
                    if (errorCode !== 0) {
                        reject(this.getAskarError(errorCode));
                    }
                    else {
                        resolve(result);
                    }
                }, koffi.pointer(callbackType));
                const errorCode = method(registeredCallback, this.callbackId++);
                if (errorCode !== 0) {
                    clearImmediate(keepAlive);
                    koffi.unregister(registeredCallback);
                    reject(this.getAskarError(errorCode));
                }
            });
        };
    }
    /**
     * Fetch the error from the native library and throw it as a JS error
     *
     * NOTE:
     * Checks whether the error code of the returned error matches the error code that was passed to the function.
     * If it doesn't, we throw an error with the original errorCode, and a custom message explaining we weren't able
     * to retrieve the error message from the native library. This should however not break functionality as long as
     * error codes are used rather than error messages for error handling.
     *
     */
    getAskarError(errorCode) {
        const error = this.getCurrentError();
        if (error.code !== errorCode) {
            return new askar_shared_1.AskarError({
                code: errorCode,
                message: 'Error details have already been overwritten on the native side, unable to retrieve error message for the error',
            });
        }
        return new askar_shared_1.AskarError(error);
    }
    handleError(errorCode) {
        if (errorCode === 0)
            return;
        throw this.getAskarError(errorCode);
    }
    handleListFree(options) {
        this.nativeAskar.askar_handle_list_free(options.handleList);
    }
    get nativeAskar() {
        return (0, library_1.getNativeAskar)();
    }
    version() {
        return this.nativeAskar.askar_version();
    }
    getCurrentError() {
        const error = (0, ffi_1.allocateStringBuffer)();
        this.nativeAskar.askar_get_current_error(error);
        const serializedError = handleReturnPointer(error);
        return JSON.parse(serializedError);
    }
    clearCustomLogger() {
        this.nativeAskar.askar_clear_custom_logger();
    }
    setCustomLogger({ logLevel, flush = false, enabled = false, logger }) {
        const registeredCallback = koffi.register(logger, ffi_1.FFI_CALLBACK_LOG);
        const errorCode = this.nativeAskar.askar_set_custom_logger(0, registeredCallback, +enabled, +flush, logLevel);
        this.handleError(errorCode);
    }
    setDefaultLogger() {
        const errorCode = this.nativeAskar.askar_set_default_logger();
        this.handleError(errorCode);
    }
    setMaxLogLevel(options) {
        const { logLevel } = (0, ffi_1.serializeArguments)(options);
        const errorCode = this.nativeAskar.askar_set_max_log_level(logLevel);
        this.handleError(errorCode);
    }
    argon2DerivePassword(options) {
        var _a;
        const { parameters, password, salt } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateSecretBuffer)();
        const errorCode = this.nativeAskar.askar_argon2_derive_password(parameters, password, salt, 
        // NOTE: we should not serialize the config, it should be passed as struct
        (_a = options.config) !== null && _a !== void 0 ? _a : null, ret);
        this.handleError(errorCode);
        const byteBuffer = handleReturnPointer(ret);
        const uint8Array = (0, ffi_1.secretBufferToUint8Array)(byteBuffer);
        this.nativeAskar.askar_buffer_free(byteBuffer);
        return uint8Array;
    }
    entryListCount(options) {
        const { entryListHandle } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateInt32Buffer)();
        const errorCode = this.nativeAskar.askar_entry_list_count(entryListHandle, ret);
        this.handleError(errorCode);
        return handleReturnPointer(ret);
    }
    entryListFree(options) {
        const { entryListHandle } = (0, ffi_1.serializeArguments)(options);
        this.nativeAskar.askar_entry_list_free(entryListHandle);
    }
    entryListGetCategory(options) {
        const { entryListHandle, index } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateStringBuffer)();
        const errorCode = this.nativeAskar.askar_entry_list_get_category(entryListHandle, index, ret);
        this.handleError(errorCode);
        return handleReturnPointer(ret);
    }
    entryListGetName(options) {
        const { entryListHandle, index } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateStringBuffer)();
        const errorCode = this.nativeAskar.askar_entry_list_get_name(entryListHandle, index, ret);
        this.handleError(errorCode);
        return handleReturnPointer(ret);
    }
    entryListGetTags(options) {
        const { entryListHandle, index } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateStringBuffer)();
        const errorCode = this.nativeAskar.askar_entry_list_get_tags(entryListHandle, index, ret);
        this.handleError(errorCode);
        return handleNullableReturnPointer(ret);
    }
    entryListGetValue(options) {
        const { entryListHandle, index } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateSecretBuffer)();
        const errorCode = this.nativeAskar.askar_entry_list_get_value(entryListHandle, index, ret);
        this.handleError(errorCode);
        const byteBuffer = handleReturnPointer(ret);
        const uint8Array = (0, ffi_1.secretBufferToUint8Array)(byteBuffer);
        this.nativeAskar.askar_buffer_free(byteBuffer);
        return uint8Array;
    }
    keyAeadDecrypt(options) {
        var _a, _b;
        const { ciphertext, localKeyHandle, nonce, aad, tag } = (0, ffi_1.serializeArguments)({
            localKeyHandle: options.localKeyHandle,
            nonce: options.nonce,
            ciphertext: options.ciphertext,
            tag: (_a = options.tag) !== null && _a !== void 0 ? _a : new Uint8Array(0),
            aad: (_b = options.aad) !== null && _b !== void 0 ? _b : new Uint8Array(0),
        });
        const ret = (0, ffi_1.allocateSecretBuffer)();
        const errorCode = this.nativeAskar.askar_key_aead_decrypt(localKeyHandle, ciphertext, nonce, tag, aad, ret);
        this.handleError(errorCode);
        const byteBuffer = handleReturnPointer(ret);
        const uint8Array = (0, ffi_1.secretBufferToUint8Array)(byteBuffer);
        this.nativeAskar.askar_buffer_free(byteBuffer);
        return uint8Array;
    }
    keyAeadEncrypt(options) {
        var _a, _b;
        const { localKeyHandle, message, nonce, aad } = (0, ffi_1.serializeArguments)({
            localKeyHandle: options.localKeyHandle,
            message: options.message,
            nonce: (_a = options.nonce) !== null && _a !== void 0 ? _a : new Uint8Array(0),
            aad: (_b = options.aad) !== null && _b !== void 0 ? _b : new Uint8Array(0),
        });
        const ret = (0, ffi_1.allocateEncryptedBuffer)();
        const errorCode = this.nativeAskar.askar_key_aead_encrypt(localKeyHandle, message, nonce, aad, ret);
        this.handleError(errorCode);
        const encryptedBuffer = handleReturnPointer(ret);
        const encryptedBufferClass = (0, ffi_1.encryptedBufferStructToClass)(encryptedBuffer);
        this.nativeAskar.askar_buffer_free(encryptedBuffer.secretBuffer);
        return encryptedBufferClass;
    }
    keyAeadGetPadding(options) {
        const { localKeyHandle, msgLen } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateInt32Buffer)();
        const errorCode = this.nativeAskar.askar_key_aead_get_padding(localKeyHandle, msgLen, ret);
        this.handleError(errorCode);
        return handleReturnPointer(ret);
    }
    keyAeadGetParams(options) {
        const { localKeyHandle } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateAeadParams)();
        const errorCode = this.nativeAskar.askar_key_aead_get_params(localKeyHandle, ret);
        this.handleError(errorCode);
        return new askar_shared_1.AeadParams(handleReturnPointer(ret));
    }
    keyAeadRandomNonce(options) {
        const { localKeyHandle } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateSecretBuffer)();
        const errorCode = this.nativeAskar.askar_key_aead_random_nonce(localKeyHandle, ret);
        this.handleError(errorCode);
        const byteBuffer = handleReturnPointer(ret);
        const uint8Array = (0, ffi_1.secretBufferToUint8Array)(byteBuffer);
        this.nativeAskar.askar_buffer_free(byteBuffer);
        return uint8Array;
    }
    keyConvert(options) {
        const { localKeyHandle, algorithm } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocatePointer)();
        const errorCode = this.nativeAskar.askar_key_convert(localKeyHandle, algorithm, ret);
        this.handleError(errorCode);
        const handle = handleReturnPointer(ret);
        return new askar_shared_1.LocalKeyHandle(handle);
    }
    keyCryptoBox(options) {
        const { nonce, message, recipientKey, senderKey } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateSecretBuffer)();
        const errorCode = this.nativeAskar.askar_key_crypto_box(recipientKey, senderKey, message, nonce, ret);
        this.handleError(errorCode);
        const byteBuffer = handleReturnPointer(ret);
        const uint8Array = (0, ffi_1.secretBufferToUint8Array)(byteBuffer);
        this.nativeAskar.askar_buffer_free(byteBuffer);
        return uint8Array;
    }
    keyCryptoBoxOpen(options) {
        const { nonce, message, senderKey, recipientKey } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateSecretBuffer)();
        const errorCode = this.nativeAskar.askar_key_crypto_box_open(recipientKey, senderKey, message, nonce, ret);
        this.handleError(errorCode);
        const byteBuffer = handleReturnPointer(ret);
        const uint8Array = (0, ffi_1.secretBufferToUint8Array)(byteBuffer);
        this.nativeAskar.askar_buffer_free(byteBuffer);
        return uint8Array;
    }
    keyCryptoBoxRandomNonce() {
        const ret = (0, ffi_1.allocateSecretBuffer)();
        const errorCode = this.nativeAskar.askar_key_crypto_box_random_nonce(ret);
        this.handleError(errorCode);
        const byteBuffer = handleReturnPointer(ret);
        const uint8Array = (0, ffi_1.secretBufferToUint8Array)(byteBuffer);
        this.nativeAskar.askar_buffer_free(byteBuffer);
        return uint8Array;
    }
    keyCryptoBoxSeal(options) {
        const { message, localKeyHandle } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateSecretBuffer)();
        const errorCode = this.nativeAskar.askar_key_crypto_box_seal(localKeyHandle, message, ret);
        this.handleError(errorCode);
        const byteBuffer = handleReturnPointer(ret);
        const uint8Array = (0, ffi_1.secretBufferToUint8Array)(byteBuffer);
        this.nativeAskar.askar_buffer_free(byteBuffer);
        return uint8Array;
    }
    keyCryptoBoxSealOpen(options) {
        const { ciphertext, localKeyHandle } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateSecretBuffer)();
        const errorCode = this.nativeAskar.askar_key_crypto_box_seal_open(localKeyHandle, ciphertext, ret);
        this.handleError(errorCode);
        const byteBuffer = handleReturnPointer(ret);
        const uint8Array = (0, ffi_1.secretBufferToUint8Array)(byteBuffer);
        this.nativeAskar.askar_buffer_free(byteBuffer);
        return uint8Array;
    }
    keyDeriveEcdh1pu(options) {
        var _a;
        const { senderKey, recipientKey, algorithm, algId, apu, apv, ephemeralKey, receive, ccTag } = (0, ffi_1.serializeArguments)({
            senderKey: options.senderKey,
            recipientKey: options.recipientKey,
            algorithm: options.algorithm,
            algId: options.algId,
            apu: options.apu,
            apv: options.apv,
            ephemeralKey: options.ephemeralKey,
            receive: options.receive,
            ccTag: (_a = options.ccTag) !== null && _a !== void 0 ? _a : new Uint8Array(0),
        });
        const ret = (0, ffi_1.allocatePointer)();
        const errorCode = this.nativeAskar.askar_key_derive_ecdh_1pu(algorithm, ephemeralKey, senderKey, recipientKey, algId, apu, apv, ccTag, receive, ret);
        this.handleError(errorCode);
        const handle = handleReturnPointer(ret);
        return new askar_shared_1.LocalKeyHandle(handle);
    }
    keyDeriveEcdhEs(options) {
        const { receive, apv, apu, algId, recipientKey, ephemeralKey, algorithm } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocatePointer)();
        const errorCode = this.nativeAskar.askar_key_derive_ecdh_es(algorithm, ephemeralKey, recipientKey, algId, apu, apv, receive, ret);
        this.handleError(errorCode);
        const handle = handleReturnPointer(ret);
        return new askar_shared_1.LocalKeyHandle(handle);
    }
    keyEntryListCount(options) {
        const { keyEntryListHandle } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateInt32Buffer)();
        const errorCode = this.nativeAskar.askar_key_entry_list_count(keyEntryListHandle, ret);
        this.handleError(errorCode);
        return handleReturnPointer(ret);
    }
    keyEntryListFree(options) {
        const { keyEntryListHandle } = (0, ffi_1.serializeArguments)(options);
        this.nativeAskar.askar_key_entry_list_free(keyEntryListHandle);
    }
    keyEntryListGetAlgorithm(options) {
        const { keyEntryListHandle, index } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateStringBuffer)();
        const errorCode = this.nativeAskar.askar_key_entry_list_get_algorithm(keyEntryListHandle, index, ret);
        this.handleError(errorCode);
        return handleReturnPointer(ret);
    }
    keyEntryListGetMetadata(options) {
        const { keyEntryListHandle, index } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateStringBuffer)();
        const errorCode = this.nativeAskar.askar_key_entry_list_get_metadata(keyEntryListHandle, index, ret);
        this.handleError(errorCode);
        return handleNullableReturnPointer(ret);
    }
    keyEntryListGetName(options) {
        const { keyEntryListHandle, index } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateStringBuffer)();
        const errorCode = this.nativeAskar.askar_key_entry_list_get_name(keyEntryListHandle, index, ret);
        this.handleError(errorCode);
        return handleReturnPointer(ret);
    }
    keyEntryListGetTags(options) {
        const { keyEntryListHandle, index } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateStringBuffer)();
        const errorCode = this.nativeAskar.askar_key_entry_list_get_tags(keyEntryListHandle, index, ret);
        this.handleError(errorCode);
        return handleNullableReturnPointer(ret);
    }
    keyEntryListLoadLocal(options) {
        const { index, keyEntryListHandle } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocatePointer)();
        const errorCode = this.nativeAskar.askar_key_entry_list_load_local(keyEntryListHandle, index, ret);
        this.handleError(errorCode);
        const handle = handleReturnPointer(ret);
        return new askar_shared_1.LocalKeyHandle(handle);
    }
    keyFree(options) {
        const { localKeyHandle } = (0, ffi_1.serializeArguments)(options);
        this.nativeAskar.askar_key_free(localKeyHandle);
    }
    keyFromJwk(options) {
        const { jwk } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocatePointer)();
        const errorCode = this.nativeAskar.askar_key_from_jwk(jwk, ret);
        this.handleError(errorCode);
        const handle = handleReturnPointer(ret);
        return new askar_shared_1.LocalKeyHandle(handle);
    }
    keyFromKeyExchange(options) {
        const { algorithm, pkHandle, skHandle } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocatePointer)();
        const errorCode = this.nativeAskar.askar_key_from_key_exchange(algorithm, skHandle, pkHandle, ret);
        this.handleError(errorCode);
        const handle = handleReturnPointer(ret);
        return new askar_shared_1.LocalKeyHandle(handle);
    }
    keyFromPublicBytes(options) {
        const { publicKey, algorithm } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocatePointer)();
        const errorCode = this.nativeAskar.askar_key_from_public_bytes(algorithm, publicKey, ret);
        this.handleError(errorCode);
        const handle = handleReturnPointer(ret);
        return new askar_shared_1.LocalKeyHandle(handle);
    }
    keyFromSecretBytes(options) {
        const { secretKey, algorithm } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocatePointer)();
        const errorCode = this.nativeAskar.askar_key_from_secret_bytes(algorithm, secretKey, ret);
        this.handleError(errorCode);
        const handle = handleReturnPointer(ret);
        return new askar_shared_1.LocalKeyHandle(handle);
    }
    keyFromSeed(options) {
        const { algorithm, method, seed } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocatePointer)();
        const errorCode = this.nativeAskar.askar_key_from_seed(algorithm, seed, method, ret);
        this.handleError(errorCode);
        const handle = handleReturnPointer(ret);
        return new askar_shared_1.LocalKeyHandle(handle);
    }
    keyGenerate(options) {
        const { algorithm, ephemeral, keyBackend } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocatePointer)();
        const errorCode = this.nativeAskar.askar_key_generate(algorithm, keyBackend, ephemeral, ret);
        this.handleError(errorCode);
        const handle = handleReturnPointer(ret);
        return new askar_shared_1.LocalKeyHandle(handle);
    }
    keyGetAlgorithm(options) {
        const { localKeyHandle } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateStringBuffer)();
        const errorCode = this.nativeAskar.askar_key_get_algorithm(localKeyHandle, ret);
        this.handleError(errorCode);
        return handleReturnPointer(ret);
    }
    keyGetEphemeral(options) {
        const { localKeyHandle } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateInt32Buffer)();
        const errorCode = this.nativeAskar.askar_key_get_ephemeral(localKeyHandle, ret);
        this.handleError(errorCode);
        return handleReturnPointer(ret);
    }
    keyGetJwkPublic(options) {
        const { localKeyHandle, algorithm } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateStringBuffer)();
        const errorCode = this.nativeAskar.askar_key_get_jwk_public(localKeyHandle, algorithm, ret);
        this.handleError(errorCode);
        return handleReturnPointer(ret);
    }
    keyGetJwkSecret(options) {
        const { localKeyHandle } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateSecretBuffer)();
        const errorCode = this.nativeAskar.askar_key_get_jwk_secret(localKeyHandle, ret);
        this.handleError(errorCode);
        const byteBuffer = handleReturnPointer(ret);
        const uint8Array = (0, ffi_1.secretBufferToUint8Array)(byteBuffer);
        this.nativeAskar.askar_buffer_free(byteBuffer);
        return uint8Array;
    }
    keyGetJwkThumbprint(options) {
        const { localKeyHandle, algorithm } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateStringBuffer)();
        const errorCode = this.nativeAskar.askar_key_get_jwk_thumbprint(localKeyHandle, algorithm, ret);
        this.handleError(errorCode);
        return handleReturnPointer(ret);
    }
    keyGetPublicBytes(options) {
        const { localKeyHandle } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateSecretBuffer)();
        const errorCode = this.nativeAskar.askar_key_get_public_bytes(localKeyHandle, ret);
        this.handleError(errorCode);
        const byteBuffer = handleReturnPointer(ret);
        const uint8Array = (0, ffi_1.secretBufferToUint8Array)(byteBuffer);
        this.nativeAskar.askar_buffer_free(byteBuffer);
        return uint8Array;
    }
    keyGetSecretBytes(options) {
        const { localKeyHandle } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateSecretBuffer)();
        const errorCode = this.nativeAskar.askar_key_get_secret_bytes(localKeyHandle, ret);
        this.handleError(errorCode);
        const byteBuffer = handleReturnPointer(ret);
        const uint8Array = (0, ffi_1.secretBufferToUint8Array)(byteBuffer);
        this.nativeAskar.askar_buffer_free(byteBuffer);
        return uint8Array;
    }
    keySignMessage(options) {
        const { localKeyHandle, message, sigType } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateSecretBuffer)();
        const errorCode = this.nativeAskar.askar_key_sign_message(localKeyHandle, message, sigType, ret);
        this.handleError(errorCode);
        const byteBuffer = handleReturnPointer(ret);
        const uint8Array = (0, ffi_1.secretBufferToUint8Array)(byteBuffer);
        this.nativeAskar.askar_buffer_free(byteBuffer);
        return uint8Array;
    }
    keyUnwrapKey(options) {
        var _a, _b;
        const { localKeyHandle, algorithm, ciphertext, nonce, tag } = (0, ffi_1.serializeArguments)({
            localKeyHandle: options.localKeyHandle,
            algorithm: options.algorithm,
            ciphertext: options.ciphertext,
            nonce: (_a = options.nonce) !== null && _a !== void 0 ? _a : new Uint8Array(0),
            tag: (_b = options.tag) !== null && _b !== void 0 ? _b : new Uint8Array(0),
        });
        const ret = (0, ffi_1.allocatePointer)();
        const errorCode = this.nativeAskar.askar_key_unwrap_key(localKeyHandle, algorithm, ciphertext, nonce, tag, ret);
        this.handleError(errorCode);
        const handle = handleReturnPointer(ret);
        return new askar_shared_1.LocalKeyHandle(handle);
    }
    keyVerifySignature(options) {
        const { localKeyHandle, sigType, message, signature } = (0, ffi_1.serializeArguments)(options);
        const ret = (0, ffi_1.allocateInt8Buffer)();
        const errorCode = this.nativeAskar.askar_key_verify_signature(localKeyHandle, message, signature, sigType, ret);
        this.handleError(errorCode);
        return Boolean(handleReturnPointer(ret));
    }
    keyWrapKey(options) {
        var _a;
        const { localKeyHandle, nonce, other } = (0, ffi_1.serializeArguments)(Object.assign(Object.assign({}, options), { nonce: (_a = options.nonce) !== null && _a !== void 0 ? _a : new Uint8Array(0) }));
        const ret = (0, ffi_1.allocateEncryptedBuffer)();
        const errorCode = this.nativeAskar.askar_key_wrap_key(localKeyHandle, other, nonce, ret);
        this.handleError(errorCode);
        const encryptedBuffer = handleReturnPointer(ret);
        const encryptedBufferClass = (0, ffi_1.encryptedBufferStructToClass)(encryptedBuffer);
        this.nativeAskar.askar_buffer_free(encryptedBuffer.secretBuffer);
        return encryptedBufferClass;
    }
    keyGetSupportedBackends() {
        const stringListHandlePtr = (0, ffi_1.allocateStringListHandle)();
        const keyGetSupportedBackendsErrorCode = this.nativeAskar.askar_key_get_supported_backends(stringListHandlePtr);
        this.handleError(keyGetSupportedBackendsErrorCode);
        const stringListHandle = stringListHandlePtr[0];
        const listCountPtr = (0, ffi_1.allocateInt32Buffer)();
        const stringListCountErrorCode = this.nativeAskar.askar_string_list_count(stringListHandle, listCountPtr);
        this.handleError(stringListCountErrorCode);
        const count = listCountPtr[0];
        const supportedBackends = [];
        for (let i = 0; i < count; i++) {
            const strPtr = (0, ffi_1.allocateStringBuffer)();
            const errorCode = this.nativeAskar.askar_string_list_get_item(stringListHandle, i, strPtr);
            this.handleError(errorCode);
            supportedBackends.push(strPtr[0]);
        }
        this.nativeAskar.askar_string_list_free(stringListHandle);
        return supportedBackends;
    }
    scanFree(options) {
        const { scanHandle } = (0, ffi_1.serializeArguments)(options);
        const errorCode = this.nativeAskar.askar_scan_free(scanHandle);
        this.handleError(errorCode);
    }
    async scanNext(options) {
        const { scanHandle } = (0, ffi_1.serializeArguments)(options);
        const handle = await this.promisify((cb, cbId) => this.nativeAskar.askar_scan_next(scanHandle, cb, cbId), ffi_1.FFI_CALLBACK_ENTRY_LIST_HANDLE);
        return askar_shared_1.EntryListHandle.fromHandle(handle);
    }
    async scanStart(options) {
        var _a, _b, _c;
        const { category, limit, offset, profile, storeHandle, tagFilter, orderBy, descending } = (0, ffi_1.serializeArguments)(Object.assign(Object.assign({}, options), { offset: (_a = options.offset) !== null && _a !== void 0 ? _a : 0, limit: (_b = options.limit) !== null && _b !== void 0 ? _b : -1, descending: (_c = options.descending) !== null && _c !== void 0 ? _c : 0 }));
        const handle = await this.promisify((cb, cbId) => this.nativeAskar.askar_scan_start(storeHandle, profile, category, tagFilter, offset, limit, orderBy, descending, cb, cbId), ffi_1.FFI_CALLBACK_SCAN_HANDLE);
        return askar_shared_1.ScanHandle.fromHandle(handle);
    }
    async sessionClose(options) {
        const { commit, sessionHandle } = (0, ffi_1.serializeArguments)(options);
        await this.promisify((cb, cbId) => this.nativeAskar.askar_session_close(sessionHandle, commit, cb, cbId), ffi_1.FFI_CALLBACK_NO_RESULT);
    }
    async sessionCount(options) {
        const { sessionHandle, tagFilter, category } = (0, ffi_1.serializeArguments)(options);
        const response = await this.promisify((cb, cbId) => this.nativeAskar.askar_session_count(sessionHandle, category, tagFilter, cb, cbId), ffi_1.FFI_CALLBACK_INT64);
        return (0, askar_shared_1.handleInvalidNullResponse)(response);
    }
    async sessionFetch(options) {
        const { name, category, sessionHandle, forUpdate } = (0, ffi_1.serializeArguments)(options);
        const handle = await this.promisify((cb, cbId) => this.nativeAskar.askar_session_fetch(sessionHandle, category, name, forUpdate, cb, cbId), ffi_1.FFI_CALLBACK_ENTRY_LIST_HANDLE);
        return askar_shared_1.EntryListHandle.fromHandle(handle);
    }
    async sessionFetchAll(options) {
        var _a, _b;
        const { forUpdate, sessionHandle, tagFilter, limit, category, orderBy, descending } = (0, ffi_1.serializeArguments)(Object.assign(Object.assign({}, options), { limit: (_a = options.limit) !== null && _a !== void 0 ? _a : -1, descending: (_b = options.descending) !== null && _b !== void 0 ? _b : 0 }));
        const handle = await this.promisify((cb, cbId) => this.nativeAskar.askar_session_fetch_all(sessionHandle, category, tagFilter, limit, orderBy, descending, forUpdate, cb, cbId), ffi_1.FFI_CALLBACK_ENTRY_LIST_HANDLE);
        return askar_shared_1.EntryListHandle.fromHandle(handle);
    }
    async sessionFetchAllKeys(options) {
        var _a;
        const { forUpdate, limit, tagFilter, sessionHandle, algorithm, thumbprint } = (0, ffi_1.serializeArguments)(Object.assign(Object.assign({}, options), { limit: (_a = options.limit) !== null && _a !== void 0 ? _a : -1 }));
        const handle = await this.promisify((cb, cbId) => this.nativeAskar.askar_session_fetch_all_keys(sessionHandle, algorithm, thumbprint, tagFilter, limit, forUpdate, cb, cbId), ffi_1.FFI_CALLBACK_KEY_ENTRY_LIST_HANDLE);
        return askar_shared_1.KeyEntryListHandle.fromHandle(handle);
    }
    async sessionFetchKey(options) {
        const { forUpdate, sessionHandle, name } = (0, ffi_1.serializeArguments)(options);
        const handle = await this.promisify((cb, cbId) => this.nativeAskar.askar_session_fetch_key(sessionHandle, name, forUpdate, cb, cbId), ffi_1.FFI_CALLBACK_KEY_ENTRY_LIST_HANDLE);
        return askar_shared_1.KeyEntryListHandle.fromHandle(handle);
    }
    async sessionInsertKey(options) {
        var _a;
        const { name, sessionHandle, expiryMs, localKeyHandle, metadata, tags } = (0, ffi_1.serializeArguments)(Object.assign(Object.assign({}, options), { expiryMs: (_a = options.expiryMs) !== null && _a !== void 0 ? _a : -1 }));
        await this.promisify((cb, cbId) => this.nativeAskar.askar_session_insert_key(sessionHandle, localKeyHandle, name, metadata, tags, expiryMs, cb, cbId), ffi_1.FFI_CALLBACK_NO_RESULT);
    }
    async sessionRemoveAll(options) {
        const { sessionHandle, tagFilter, category } = (0, ffi_1.serializeArguments)(options);
        const response = await this.promisify((cb, cbId) => this.nativeAskar.askar_session_remove_all(sessionHandle, category, tagFilter, cb, cbId), ffi_1.FFI_CALLBACK_INT64);
        return (0, askar_shared_1.handleInvalidNullResponse)(response);
    }
    async sessionRemoveKey(options) {
        const { sessionHandle, name } = (0, ffi_1.serializeArguments)(options);
        await this.promisify((cb, cbId) => this.nativeAskar.askar_session_remove_key(sessionHandle, name, cb, cbId), ffi_1.FFI_CALLBACK_NO_RESULT);
    }
    async sessionStart(options) {
        const { storeHandle, profile, asTransaction } = (0, ffi_1.serializeArguments)(options);
        const handle = await this.promisify((cb, cbId) => this.nativeAskar.askar_session_start(storeHandle, profile, asTransaction, cb, cbId), ffi_1.FFI_CALLBACK_SESSION_HANDLE);
        return askar_shared_1.SessionHandle.fromHandle(handle);
    }
    async sessionUpdate(options) {
        var _a, _b;
        const { name, sessionHandle, category, expiryMs, tags, operation, value } = (0, ffi_1.serializeArguments)(Object.assign(Object.assign({}, options), { expiryMs: (_a = options.expiryMs) !== null && _a !== void 0 ? _a : -1, value: (_b = options.value) !== null && _b !== void 0 ? _b : new Uint8Array(0) }));
        await this.promisify((cb, cbId) => this.nativeAskar.askar_session_update(sessionHandle, operation, category, name, value, tags, expiryMs, cb, cbId), ffi_1.FFI_CALLBACK_NO_RESULT);
    }
    async sessionUpdateKey(options) {
        var _a;
        const { expiryMs, tags, name, sessionHandle, metadata } = (0, ffi_1.serializeArguments)(Object.assign(Object.assign({}, options), { expiryMs: (_a = options.expiryMs) !== null && _a !== void 0 ? _a : -1 }));
        await this.promisify((cb, cbId) => this.nativeAskar.askar_session_update_key(sessionHandle, name, metadata, tags, expiryMs, cb, cbId), ffi_1.FFI_CALLBACK_NO_RESULT);
    }
    async storeClose(options) {
        const { storeHandle } = (0, ffi_1.serializeArguments)(options);
        await this.promisify((cb, cbId) => this.nativeAskar.askar_store_close(storeHandle, cb, cbId), ffi_1.FFI_CALLBACK_NO_RESULT);
    }
    async storeCopyTo(options) {
        var _a;
        const { storeHandle, targetUri, passKey, keyMethod, recreate } = (0, ffi_1.serializeArguments)(Object.assign(Object.assign({}, options), { recreate: (_a = options.recreate) !== null && _a !== void 0 ? _a : 0 }));
        const response = await this.promisify((cb, cbId) => this.nativeAskar.askar_store_copy(storeHandle, targetUri, keyMethod, passKey, recreate, cb, cbId), ffi_1.FFI_CALLBACK_STORE_HANDLE);
        return askar_shared_1.StoreHandle.fromHandle(response);
    }
    async storeCreateProfile(options) {
        const { storeHandle, profile } = (0, ffi_1.serializeArguments)(options);
        const response = await this.promisify((cb, cbId) => this.nativeAskar.askar_store_create_profile(storeHandle, profile, cb, cbId), ffi_1.FFI_CALLBACK_STRING);
        return (0, askar_shared_1.handleInvalidNullResponse)(response);
    }
    storeGenerateRawKey(options) {
        var _a;
        const { seed } = (0, ffi_1.serializeArguments)(Object.assign(Object.assign({}, options), { seed: (_a = options.seed) !== null && _a !== void 0 ? _a : new Uint8Array(0) }));
        const ret = (0, ffi_1.allocateStringBuffer)();
        const errorCode = this.nativeAskar.askar_store_generate_raw_key(seed, ret);
        this.handleError(errorCode);
        return ret[0];
    }
    async storeGetDefaultProfile(options) {
        const { storeHandle } = (0, ffi_1.serializeArguments)(options);
        const response = await this.promisify((cb, cbId) => this.nativeAskar.askar_store_get_default_profile(storeHandle, cb, cbId), ffi_1.FFI_CALLBACK_STRING);
        return (0, askar_shared_1.handleInvalidNullResponse)(response);
    }
    async storeGetProfileName(options) {
        const { storeHandle } = (0, ffi_1.serializeArguments)(options);
        const response = await this.promisify((cb, cbId) => this.nativeAskar.askar_store_get_profile_name(storeHandle, cb, cbId), ffi_1.FFI_CALLBACK_STRING);
        return (0, askar_shared_1.handleInvalidNullResponse)(response);
    }
    async storeListProfiles(options) {
        const { storeHandle } = (0, ffi_1.serializeArguments)(options);
        const listHandle = await this.promisify((cb, cbId) => this.nativeAskar.askar_store_list_profiles(storeHandle, cb, cbId), ffi_1.FFI_CALLBACK_STRING_LIST_HANDLE);
        if (listHandle === null) {
            throw askar_shared_1.AskarError.customError({ message: 'Invalid handle' });
        }
        const counti32 = (0, ffi_1.allocateInt32Buffer)();
        const errorCode = this.nativeAskar.askar_string_list_count(listHandle, counti32);
        this.handleError(errorCode);
        const count = counti32[0];
        const ret = [];
        const strval = (0, ffi_1.allocateStringBuffer)();
        for (let i = 0; i < count; i++) {
            const errorCode = this.nativeAskar.askar_string_list_get_item(listHandle, i, strval);
            this.handleError(errorCode);
            ret.push(strval[0]);
        }
        this.nativeAskar.askar_string_list_free(listHandle);
        return ret;
    }
    async storeListScans(options) {
        const { storeHandle } = (0, ffi_1.serializeArguments)(options);
        const handleList = await this.promisify((cb, cbId) => this.nativeAskar.askar_store_list_scans(storeHandle, cb, cbId), ffi_1.FFI_CALLBACK_HANDLE_LIST);
        if (!handleList)
            return [];
        const scanHandleList = (0, ffi_1.decodeHandleList)(handleList).map((scanHandle) => askar_shared_1.ScanHandle.fromHandle(scanHandle));
        this.handleListFree({ handleList });
        return scanHandleList;
    }
    async storeListSessions(options) {
        const { storeHandle } = (0, ffi_1.serializeArguments)(options);
        const handleList = await this.promisify((cb, cbId) => this.nativeAskar.askar_store_list_sessions(storeHandle, cb, cbId), ffi_1.FFI_CALLBACK_HANDLE_LIST);
        if (!handleList)
            return [];
        const sessionHandleList = (0, ffi_1.decodeHandleList)(handleList).map((sessionHandle) => askar_shared_1.SessionHandle.fromHandle(sessionHandle));
        this.handleListFree({ handleList });
        return sessionHandleList;
    }
    async storeOpen(options) {
        const { profile, keyMethod, passKey, specUri } = (0, ffi_1.serializeArguments)(options);
        const handle = await this.promisify((cb, cbId) => this.nativeAskar.askar_store_open(specUri, keyMethod, passKey, profile, cb, cbId), ffi_1.FFI_CALLBACK_STORE_HANDLE);
        return askar_shared_1.StoreHandle.fromHandle(handle);
    }
    async storeProvision(options) {
        const { profile, passKey, keyMethod, specUri, recreate } = (0, ffi_1.serializeArguments)(options);
        const handle = await this.promisify((cb, cbId) => this.nativeAskar.askar_store_provision(specUri, keyMethod, passKey, profile, recreate, cb, cbId), ffi_1.FFI_CALLBACK_STORE_HANDLE);
        return askar_shared_1.StoreHandle.fromHandle(handle);
    }
    async storeRekey(options) {
        const { passKey, keyMethod, storeHandle } = (0, ffi_1.serializeArguments)(options);
        await this.promisify((cb, cbId) => this.nativeAskar.askar_store_rekey(storeHandle, keyMethod, passKey, cb, cbId), ffi_1.FFI_CALLBACK_NO_RESULT);
    }
    async storeRemove(options) {
        const { specUri } = (0, ffi_1.serializeArguments)(options);
        const response = await this.promisify((cb, cbId) => this.nativeAskar.askar_store_remove(specUri, cb, cbId), ffi_1.FFI_CALLBACK_INT8);
        return (0, askar_shared_1.handleInvalidNullResponse)(response);
    }
    async storeRemoveProfile(options) {
        const { storeHandle, profile } = (0, ffi_1.serializeArguments)(options);
        const response = await this.promisify((cb, cbId) => this.nativeAskar.askar_store_remove_profile(storeHandle, profile, cb, cbId), ffi_1.FFI_CALLBACK_INT8);
        return (0, askar_shared_1.handleInvalidNullResponse)(response);
    }
    async storeSetDefaultProfile(options) {
        const { storeHandle, profile } = (0, ffi_1.serializeArguments)(options);
        await this.promisify((cb, cbId) => this.nativeAskar.askar_store_set_default_profile(storeHandle, profile, cb, cbId), ffi_1.FFI_CALLBACK_NO_RESULT);
    }
    async storeRenameProfile(options) {
        const { storeHandle, fromProfile, toProfile } = (0, ffi_1.serializeArguments)(options);
        const response = await this.promisify((cb, cbId) => this.nativeAskar.askar_store_rename_profile(storeHandle, fromProfile, toProfile, cb, cbId), ffi_1.FFI_CALLBACK_INT8);
        return (0, askar_shared_1.handleInvalidNullResponse)(response);
    }
    async storeCopyProfile(options) {
        const { fromHandle, toHandle, fromProfile, toProfile } = (0, ffi_1.serializeArguments)(options);
        await this.promisify((cb, cbId) => this.nativeAskar.askar_store_copy_profile(fromHandle, toHandle, fromProfile, toProfile, cb, cbId), ffi_1.FFI_CALLBACK_NO_RESULT);
    }
    async migrateIndySdk(options) {
        const { specUri, kdfLevel, walletKey, walletName } = (0, ffi_1.serializeArguments)(options);
        await this.promisify((cb, cbId) => this.nativeAskar.askar_migrate_indy_sdk(specUri, walletName, walletKey, kdfLevel, cb, cbId), ffi_1.FFI_CALLBACK_NO_RESULT);
    }
}
exports.NodeJSAskar = NodeJSAskar;
//# sourceMappingURL=NodeJSAskar.js.map