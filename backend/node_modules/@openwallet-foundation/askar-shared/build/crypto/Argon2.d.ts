export declare enum Argon2Parameters {
    Moderate = 0,
    Interactive = 1,
    Custom = -1
}
export declare enum Argon2Algorithm {
    Argon2d = 0,
    Argon2i = 1,
    Argon2id = 2
}
export declare enum Argon2Version {
    V0x10 = 16,
    V0x13 = 19
}
export type Argon2Config = {
    algorithm: Argon2Algorithm;
    version: Argon2Version;
    parallelism: number;
    memCost: number;
    timeCost: number;
};
export declare class Argon2 {
    static derivePassword(parameters: Argon2Parameters | Argon2Config, password: Uint8Array, salt: Uint8Array): Uint8Array;
}
